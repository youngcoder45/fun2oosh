"""
Configuration management for the Fun2Oosh Economy Bot.

The canonical definition lives in `utils/config.py` — this module re-exports
it so both `import config` and `from utils.config import Config` work.
"""

from utils.config import Config

__all__ = ['Config']
